"""Inference Runtime infrastructure — minimal sprint: version constant only."""

from __future__ import annotations

# Stamped into every ExecutionTrace so results are traceable to a runtime release.
RUNTIME_VERSION = "1.0.0"
